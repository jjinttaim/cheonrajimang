# 추천 계산의 시간·표집 해상도 검증 계획

2026-09-19. 결과를 계산하기 전에 아래 입력과 비교를 고정한다. 이는 **수치 근사 검사**이며 실제 실종자 행동/탐지 성능 평가나 추가 학습이 아니다. 실제 한림 지형과 현재 학습된 수색대 속도를 사용하지만 임무 입력·대상 이동은 합성 가정이다.

## 고정 비교

- 공통: IPP 126.268 E, 33.397 N; 오차 가정 50m; seed 42; 3개 기존 시나리오; 같은 위치에서 출발하는 sweep 2팀; 계획 30분; 시설 가중 없음; 탐지폭 8/15/22m.
- DAY: 경과시간 가정 0.5h, 기준시각 2026-09-19T14:00:00+09:00, 계획 시작 +600초.
- DUSK: 경과시간 가정 2h, 기준시각 2026-09-19T18:00:00+09:00, 계획 시작 +900초. 야간 이동 가정 0.6. 태양고도 계산은 기존 코드 그대로 사용한다.
- 기존 60초/8,192 표집과 30초/8,192, 15초/8,192, 60초/4,096, 60초/2,048을 비교한다.
- 비교 기준은 원래 모든 가중 경로 토큰을 사용하는 15초 및 5초 기록이다. 전 경로라도 시뮬레이션 가정이며 사람의 실제 경로 정답이 아니다. 5초도 연속시간 정답/전역 최적해가 아니다.
- 이동 seed와 원래 경로 토큰을 고정한다. 표집 수를 바꿀 때 이동 난수 규칙까지 바꾸지 않는다. 기존 층화 표집은 서로 중첩된 표본이 아님을 명시한다.

## 지표와 검사

1. 추천 구역·수색 셀의 변경, 같은 5초/전 경로 자료에서 각 추천을 다시 채점한 시나리오별 탐지효과 가정. 다른 근사로 계산한 자기 점수끼리만 비교하지 않는다.
2. 위 재평가에서 기준 추천 대비 최악 시나리오 효과의 **부호 있는 차이**. 기준도 제한된 후보의 탐욕 선택이므로 이를 전역 최적 대비 regret/정확도라고 부르지 않는다.
3. 시작 구역 분포 총변동거리(지도 밖 포함), 질량 합, 유효 경로 수, 실행 시간. 결과의 가정 탐지효과는 실제 발견률이 아니다.
4. 합성 반례: 60초 사이에 대상이 여러 셀을 지나갈 때 최근접 프레임 평가가 짧은 만남을 빠뜨리는지; 시간축/시각이 유한한지; 같은 물체를 여러 팀이 수색할 때 누적 효과가 질량을 초과하지 않는지.

일치율이 높은 입력만 남기지 않는다. 두 합성 임무로 국내/모든 지형에서 안정성을 증명하지 않는다. 실제 확인한 계산 결함은 반례 테스트와 함께 수정하고, 변경 전 결과도 보존한다. 고정된 비교 입력·지표는 수정하지 않는다. 현장 성능에 대한 채택 임계값을 이 실험에서 만들지 않는다.
